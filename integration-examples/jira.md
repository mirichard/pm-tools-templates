# Jira Integration Example

This example demonstrates exporting the **Risk Register** template to Jira.

1. Export `Traditional/Templates/risk_register_template.md` to CSV.
2. In Jira, navigate to **Issues > Import Issues from CSV**.
3. Map CSV columns to Jira fields (e.g., Risk ID → Issue Key).
4. Complete the import to create Jira issues for each risk.
5. Use Jira dashboards to track mitigation progress.
