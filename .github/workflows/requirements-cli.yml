name: Requirements CLI
on:
  pull_request:
    paths:
      - 'tools/requirements-structuring-cli/**'
      - '.github/workflows/requirements-cli.yml'
  push:
    branches: [main]
    paths:
      - 'tools/requirements-structuring-cli/**'
      - '.github/workflows/requirements-cli.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tools/requirements-structuring-cli
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: tools/requirements-structuring-cli/package-lock.json
      - run: npm ci
      - run: npm run validate:nfr-library
      - run: npm test
      - run: npm pack --dry-run
