name: "PM Tools Templates CodeQL Config"

# Disable the default security-extended query suite.
disable-default-queries: false

# Filter out NOTE-level (recommendation) alerts and reduce noise
query-filters:
  - exclude:
      problem.severity:
        - recommendation

# Include additional query suites and queries
queries:
  - name: security-and-quality
    uses: security-and-quality
  - name: security-extended
    uses: security-extended

# Paths to include in analysis
paths:
  - src
  - dashboard-mvp
  - tools
  - workflow-orchestration

# Paths to ignore during analysis
paths-ignore:
  - node_modules
  - "**/*.min.js"
  - "**/*.bundle.js"
  - "**/*.generated.js"
  - "**/vendor/**"
  - "**/dist/**"
  - "**/build/**"
  - "**/__tests__/**"
  - "**/test/**"
  - "**/tests/**"
  - "**/*.test.js"
  - "**/*.test.ts"
  - "**/*.spec.js"
  - "**/*.spec.ts"
  - "site/server.js"
  - "backend/"
  - "curation-dashboard/"

# Language-specific configurations
packs:
  javascript:
    # Include community packs for enhanced analysis
    - "codeql/javascript-queries"

