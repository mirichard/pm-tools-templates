{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "ucs-template.schema.json",
  "title": "Use Case Specification Template",
  "description": "Structured UCS template with basic, alternative, and exception flows per Li & Zheng (2025) Definitions 1-2",
  "type": "object",
  "required": ["useCaseId", "intent", "role", "preconditions", "postconditions", "basicFlow"],
  "properties": {
    "useCaseId": {
      "type": "string",
      "description": "Unique identifier for the use case"
    },
    "intent": {
      "type": "string",
      "description": "What this use case accomplishes"
    },
    "role": {
      "type": "string",
      "description": "Primary actor performing the use case"
    },
    "preconditions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Conditions that must hold before the use case begins"
    },
    "postconditions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Conditions that hold after successful completion"
    },
    "basicFlow": {
      "$ref": "#/definitions/flow",
      "description": "Definition 1: BF = [step1, ..., stepn] — the primary execution path"
    },
    "alternativeFlows": {
      "type": "array",
      "items": { "$ref": "#/definitions/alternativeFlow" },
      "description": "Definition 2: altF = {altFi | 1 ≤ i ≤ n} — conditional deviations from basic flow"
    },
    "exceptionFlows": {
      "type": "array",
      "items": { "$ref": "#/definitions/alternativeFlow" },
      "description": "Error paths (structurally identical to alternative flows)"
    },
    "businessObjects": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Business objects referenced in this use case"
    },
    "relatedUseCases": {
      "type": "array",
      "items": { "type": "string" },
      "description": "IDs of use cases referenced by steps in this template"
    },
    "sourceRequirements": {
      "type": "array",
      "description": "Parser-owned original source lines; optional for legacy artifacts.",
      "items": {
        "type": "object",
        "required": [
          "id",
          "originalText"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "originalText": {
            "type": "string"
          }
        }
      }
    }
  },
  "definitions": {
    "step": {
      "type": "object",
      "required": ["stepId", "actor", "action", "businessObject"],
      "properties": {
        "sourceRequirementId": {
          "type": "string",
          "description": "Originating parser requirement ID; optional for legacy artifacts."
        },
        "sourceText": {
          "type": "string",
          "description": "Exact original source line reattached by code; optional for legacy artifacts."
        },
        "stepId": {
          "type": "string",
          "description": "Step identifier (e.g., '1', '2', '3a1')"
        },
        "actor": { "type": "string" },
        "action": { "type": "string" },
        "businessObject": { "type": "string" },
        "toActor": { "type": "string" },
        "precondition": { "type": "string" },
        "postcondition": { "type": "string" },
        "refUseCaseId": {
          "type": "string",
          "description": "If this step delegates to another use case"
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the step"
        }
      }
    },
    "flow": {
      "type": "object",
      "required": ["steps"],
      "properties": {
        "steps": {
          "type": "array",
          "items": { "$ref": "#/definitions/step" },
          "minItems": 1
        }
      }
    },
    "alternativeFlow": {
      "type": "object",
      "required": ["flowId", "deviationPoint", "triggerCondition", "steps"],
      "properties": {
        "flowId": {
          "type": "string",
          "description": "Flow identifier (e.g., '3a', '4b')"
        },
        "deviationPoint": {
          "type": "string",
          "description": "The basic flow step ID this flow branches from"
        },
        "triggerCondition": {
          "type": "string",
          "description": "The condition that triggers this alternative/exception flow"
        },
        "steps": {
          "type": "array",
          "items": { "$ref": "#/definitions/step" },
          "minItems": 1
        },
        "rejoinPoint": {
          "type": "string",
          "description": "The basic flow step ID to rejoin after this flow (null if it terminates the use case)"
        }
      }
    }
  }
}
